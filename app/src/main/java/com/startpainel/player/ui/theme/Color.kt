package com.startpainel.player.ui.theme

import androidx.compose.ui.graphics.Color

// Startflix brand — vermelho streaming premium
val BrandBlue    = Color(0xFFE50914)   // Startflix red (var name mantido para compatibilidade)
val BrandBlueDim = Color(0xFF9B0A10)
val BrandAccent  = Color(0xFFFF6B35)   // laranja quente

// Canvas ultra-dark premium
val Background       = Color(0xFF080808)
val SurfaceDark      = Color(0xFF141414)
val SurfaceMid       = Color(0xFF1E1E1E)
val SurfaceLight     = Color(0xFF2A2A2A)

// Text
val OnSurface        = Color(0xFFEEEEEE)
val OnSurfaceMuted   = Color(0xFF888888)
val OutlineSoft      = Color(0xFF333333)

// Accents
val LiveRed      = Color(0xFFE50914)
val SuccessGreen = Color(0xFF2ECC71)
val ErrorRed     = Color(0xFFFF453A)

// Gradient helpers
val GradientStart = Color(0x00000000)
val GradientEnd   = Color(0xFF080808)
